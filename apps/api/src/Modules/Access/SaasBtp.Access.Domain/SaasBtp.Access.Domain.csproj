<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Access.Domain — pure center (Hexagonal). References nothing.
    Story #4 is claims-only: per ADR-ARCH-004 the Access module owns NO membership model
    (no entity, aggregate, repository, service, or schema). This project therefore holds
    only an assembly anchor for the architecture tests; a domain model appears the day a
    real Access aggregate exists.
  -->

</Project>
