**Session — 2026-06-28 (kickoff SaaS BTP)**

## Contexte de départ

Suite à la stabilisation de l'écosystème MicroKit (Tenancy renommé, Auth preview.2,
CONTEXT-DEV.md à jour). Démarrage du projet SaaS BTP.

Objectif exprimé : construire le SaaS BTP avec une organisation propre, type entreprise
augmentée par l'IA, même en solo — cible inspirée d'un "AI Operating System" à ~15 agents
collaborant via Notion / Claude Code / n8n / Git / Figma.

---

## Décisions actées

### Cap (north star) — AI OS
- Cible AI OS conservée intégralement (15 agents, double finalité produit + expertise vendable).
- Méthode : progressive, jamais big-bang ; chaque agent/connexion naît d'un besoin réel ;
  une seule connexion à la fois ; n8n seulement sur un handoff fait 5 fois à la main.
- Enregistré dans context/ai-os/ai-os-vision.md + roadmap 6 paliers dans ai-os-roadmap.md.

### Structure projet
- Calquée sur la convention MicroKit (.claude-context/sessions/ + context/).
- Deux pôles distincts : context/product/ et context/ai-os/.
- ADRs à 3 préfixes : ADR-PROD / ADR-ARCH / ADR-ORG.
- CLAUDE.md à la RACINE (convention Claude Code officielle vérifiée : auto-load +
  survit à la compaction). Pas dans .claude/.

### Mono-repo
- Décision : mono-repo unique pour le SaaS BTP (~/workspace/products/saas-btp/).
- Rationale : contexte unifié pour les agents, PR atomiques cross-app, réutilisation de
  la discipline MicroKit.
- 3 garde-fous actés : CI à path-filters par app, pipelines de deploy séparés par app,
  .gitignore strict multi-toolchain.

### Sessions versionnées (Option B)
- Les fichiers de session sont COMMITTÉS (contrairement à MicroKit) — traçabilité des
  décisions = partie de l'actif d'expertise. Plain text.
- Brouillons privés possibles dans .claude-context/sessions/local/ (gitignoré).

### ADR-ARCH-001 — frontière de contrat OpenAPI
- Décision : les types TS sont générés depuis le document OpenAPI, PAS depuis le C#.
- .NET 10 génère l'OpenAPI nativement (Swashbuckle retiré), build-time + committé.
- OpenAPI 3.1 / JSON Schema 2020-12 : génération TS sans bug sur modèles complexes.
- apps/api reste isolée du workspace pnpm. Générateur (Kiota/openapi-typescript/...) : JIT.

---

## Réalisé

### Fichiers d'ancrage générés
- CLAUDE.md (racine), .claude/agents/_TEMPLATE.md, sessions, ai-os (vision/roadmap/method),
  product (vision/scope-phase-1/backlog), architecture, decisions-index + ADR-ARCH-001.

### Squelette monorepo (via Claude Code)
- Arbo : apps/{api,web,mobile}, packages/, design/, infra/{docker,coolify,supabase},
  scripts/, docs/, .github/workflows/ — vides avec .gitkeep.
- Day-1 : .gitignore multi-toolchain, .editorconfig, pnpm-workspace.yaml
  (apps/api exclu — ADR-ARCH-001), package.json, SaasBtp.slnx vide,
  infra/docker/compose.dev.yml (placeholder), design/README.md, README.md.
- AUCUN code applicatif scaffolé (pas de dotnet new, pas de Next.js/Expo) — volontaire.

### Vérifications
- pnpm épinglé à 11.9.0 (dernière version vérifiée). Node 24.16.0 compatible (pnpm 11 = Node 22+).
- Note pnpm 11 : minimumReleaseAge=1440 par défaut (paquets < 1j non résolus) —
  désactivable via minimumReleaseAge: 0 si besoin un jour. Pas touché.
- .mcp.json gitignoré (convention CONTEXT-INFRA) — config MCP non partagée via repo. Volontaire.
- git ls-files : aucun bin/obj/node_modules/.mcp.json committé. Propre.

### Repo GitHub créé + poussé
- github.com/michaelatsey/saas-btp (privé) créé via gh CLI.
- main poussé, branche dev créée et poussée.

### Protection de branche — fallback hook client
- Ruleset serveur tenté via gh CLI -> HTTP 403 : rulesets/branch protection PAYANTS sur
  repo privé (GitHub Pro requis) ou repo public. Non retenu (repo privé, plan gratuit).
- Fallback : hook git client .githooks/pre-push bloquant les force-push sur main et dev.
- Hook TESTÉ sur 6 scénarios (premier push, fast-forward, force-push main/dev bloqués,
  branche feature autorisée) avant pose — tous OK.
- git config core.hooksPath .githooks (config locale, à refaire 1x par machine — noté README).
- Limites connues : contournable --no-verify (anti-accident, pas anti-volonté) ;
  ne couvre PAS --delete-branch côté serveur -> parade = règle absolue déjà enregistrée.

### Commits
- feat(repo): bootstrap monorepo skeleton (ancrage + squelette).
- chore(git): add pre-push hook blocking force-push on main/dev (+ note setup README).
- docs(session): close 2026-06-28 kickoff session (ce fichier).

---

## Position actuelle

- Palier AI OS : 1 (hub + agents manuels) — en cours.
- Repo github.com/michaelatsey/saas-btp (privé), main + dev poussés, hook actif.
- Scope Phase 1 et bounded contexts : NON encore définis.

---

## Prochaine étape (reprise à froid)

Définir le scope vertical Phase 1 + la décomposition en bounded contexts du
modular monolith -> remplit context/product/scope-phase-1.md et
context/architecture/architecture.md. C'est l'ancrage produit sur lequel les premiers
agents btp-* travailleront.

---

## Open items

| Item | Statut |
|------|--------|
| Scope Phase 1 (slice vertical) | prochaine étape |
| Bounded contexts modular monolith | prochaine étape |
| Hub Notion BTP (structure) | Palier 1 |
| Noyau d'agents btp-* (product, architect, implementer, reviewer, ux) | JIT Palier 1 |
| CI path-filters par app | JIT (au 1er code applicatif) |
| Choix générateur OpenAPI -> TS | JIT (à la 1re génération de client) |
| Protection branche serveur (ruleset) | si passage GitHub Pro / associé (sinon hook client suffit) |
